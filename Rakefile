desc "Run Budgeting app record adding feature with default profile"
task :add_record_chrome  do
  system('cucumber features/add_record.feature')
end
desc "Run Budgeting app table verification with default profile"
task :verify_budget_table_chrome  do
  system('cucumber features/verify_budget_table.feature')
end
desc "Run Budgeting app figures verification with default profile"
task :verify_reports_chrome  do
  system('cucumber features/verify_reports.feature')
end
